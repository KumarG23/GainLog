# Remote weekly workout plan — rollout contract

GainLog keeps completed workouts as immutable history. The app's built-in five-day template is a fallback skeleton; an API-owned override replaces the scheduled exercises for a day. The client still computes conservative, exact-template/exact-exercise recommendations from completed pre-Monday history and leaves all actual set fields blank. Wednesday remains cardio-only.

- `GET /workout-plan?weekStart=YYYY-MM-DD` (Monday, Eastern week) returns `{weekStart, revision, overrides}`. It reads the newest version effective on or before that week; no row means revision `0` and no overrides.
- `PUT /workout-plan?weekStart=YYYY-MM-DD` accepts `{expectedRevision, overrides}` for the current or next eight weeks. The complete override map is replaced atomically, not merged. Preserve other days from GET when editing one. A stale revision returns `409`; reload and reconcile, never blindly retry. The response is the saved revision and validated plan. An explicit edit to the current week is permitted; future weeks inherit the newest version unless overridden.
- Each overridden lifting day contains the complete ordered list of strength exercises, each with `name`, `sets`, `targetReps`, `rest`, optional `cue`, and exactly two `substitutions`. Names are exact history identities; do not alias machine variants. Recovery cannot contain strength exercises. The optional elliptical finisher and Wednesday easy cardio remain built into the app.
- The app loads workout history and plan together on entry to Log and fails closed when an enabled remote plan request fails. Re-select a draft after an explicit plan edit; an in-progress draft is never silently overwritten. A compatible V2 Android build still needs `EXPO_PUBLIC_GAINLOG_REMOTE_PLAN=1`; later plan-data edits need no APK.

## Rollout status

- Backend: deployed on VM 111 from `release/workout-plan-live-models` commit `ffac52c`, preserving the live coach-model choices and existing Journey route. Source SHA-256: `09887de1daa46189d632411bf4ee9613bd34452e123a1fe4517503605d8e0023`; prior source rollback at `/opt/gainlog/release-backups/workout-plan-ffac52c-20260924/`. Pre- and post-deploy PostgreSQL backups were independently restore-tested and checksum-verified; the post-plan archive is `/mnt/gainlog-backups/pre-upgrade/gainlog-20260924T135809Z.dump`.
- Live: `/health`, `/workout-plan`, Journey, and `/workouts/` returned HTTP 200. Week of 2026-09-21 is revision 1 with Standing Calf Raise in Legs; 2026-09-28 inherits it. A stale PUT returned 409 and preserved revision 1.
- **Revoked phone candidate:** the stable-main-line `1.1.24`/code `41` APK was staged at `Downloads/Jarvis-APK/GainLog-1.1.24-remote-weekly-plan-arm64.apk` before Neal clarified that he uses the V2 app. Do **not** install it: it would regress his V2 interface and Baseline G icon. The temporary download URL has been removed, but the bridge cannot remove the file already copied to the phone. The canonical local artifact is retained for provenance, not distribution.
- The merged V2 + remote-plan Android build `1.1.25`/code `42` was built from main commit `980e203` with both feature flags and the production tailnet API. The signed ARM64 APK is retained at `/home/neal/code/GainLog/releases/gainlog/GainLog-1.1.25-v2-remote-weekly-plan-arm64.apk`, SHA-256 `6f6c8d1eacba9314e4aba1167d99173e44cc9a26b642c87f50ad3c3afef11898` (50,273,461 bytes). Its manifest, APK v2 signature, signer continuity with 1.1.23, ZIP alignment, remote-plan bundle endpoint, V2 branding, and *all* packaged V2 icon PNG/WebP resources matched the 1.1.23 V2 line. The Pixel action bridge acknowledged a checksum-matched copy at `Downloads/Jarvis-APK/GainLog-1.1.25-v2-remote-weekly-plan-arm64.apk`; the temporary serving route was removed. **Installation and physical V2/workout-plan acceptance remain pending Neal's Android confirmation.** Do not install the separate revoked 1.1.24 stable-line file. The VM web frontend remains on older source and has not been updated.

The current Legs identity is `Standing Calf Raise` (historic Thursday Legs log), not `Leg Press Calf Raise`. Its prior load is a reference, not a mandate after notable soreness. Client-side progression remains conservative and cannot infer clean form or RIR from a sore-muscle report alone.
