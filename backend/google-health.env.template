# Google Health server credential enrollment (no values here)
# Add these names to the protected host-only /etc/gainlog.env; never commit values.
GAINLOG_GOOGLE_HEALTH_CLIENT_ID=
GAINLOG_GOOGLE_HEALTH_CLIENT_SECRET=
# Generate separately on the server: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
GAINLOG_GOOGLE_HEALTH_TOKEN_ENCRYPTION_KEY=
